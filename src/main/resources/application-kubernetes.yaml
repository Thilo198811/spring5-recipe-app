spring:
  datasource:
    url: jdbc:mysql://recipe-mysql:3306/recipe-db
    username: user
    password: password
    
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true